<view wx:for="{{item}}"class="title">
  <image class="avatar" mode="aspectFit" src="{{item.user_image}}"> </image>
  <view class="desc">
      <view class="item">{{item.title}}</view>
      <view class="time">{{item.edittime}}</view>
  </view>
</view>
<view class='content'>{{content}}
</view>